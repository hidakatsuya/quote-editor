name: System Test

on: push

jobs:
  system_test:
    name: System Test

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: ruby/setup-ruby@v1
      with:
        ruby-version: "3.1.0"
    - run: bin/setup
    - run: bin/rails test:system

